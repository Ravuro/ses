<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- Android 10+ Wi-Fi Direct icin konum izni sarti kosuyor. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:label="Telsiz"
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name="com.ravuro.telsiz.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name="com.ravuro.telsiz.TelsizService"
            android:exported="false" />

        <!--
          Nabiz alicisi. Manifeste yaziliyor cunku surec olduruldukten sonra
          da alarmi alabilmesi, yani telsizi geri getirebilmesi gerekiyor;
          calisma aninda kaydedilen bir alici bunu yapamaz.
        -->
        <receiver
            android:name="com.ravuro.telsiz.Watchdog"
            android:exported="false">
            <intent-filter>
                <action android:name="com.ravuro.telsiz.NABIZ" />
            </intent-filter>
        </receiver>

        <!--
          Ses tuşunu sistem seviyesinde yakalar. Bazı üretici arayüzleri
          medya oturumu yolunu çalıştırmadığı için gerekiyor; kullanıcı
          Ayarlar'dan elle açmadıkça devreye girmez.
        -->
        <service
            android:name="com.ravuro.telsiz.KeyService"
            android:exported="true"
            android:label="Telsiz ses tuşu"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/erisim" />
        </service>
    </application>
</manifest>
